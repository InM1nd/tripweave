# Backlog — что доработать

Сводка по анализу проекта: mock-данные, незавершённые фазы, документация, качество. Обновлять по мере выполнения.

---

## 1. Mock-данные и заглушки

| Место | Проблема |
|-------|----------|
| **Dashboard** | «Places Saved» захардкожено как `47` — нет привязки к реальным сохранённым местам. |
| **Profile** | Пользователь и поездки из `mockTrips` / mock user — не из БД/сессии. |
| **Notifications** | Список из `mockNotifications` — нет модели/API и реальных уведомлений. |
| **Explore** | Для «upcoming trip» и рекомендаций используются `mockTrips` и мок — не реальный следующий поезд пользователя. |
| **TripMap** | Карта строится по `mockLocations`, а не по координатам событий поездки (`event.lat/lng`). |
| **GlobalMap** | То же — mock-данные вместо реальных поездок/городов. |
| **TripHeader** | При отсутствии обложки — `placeholder-trip.jpg`; проверить наличие файла в `public/`. |

---

## 2. Функциональность (по docs/tasks.md и plan.md)

### Не сделано

- **Phase 9 — Notifications**  
  Нет реальной модели/API уведомлений, создания при действиях (приглашение, новое событие и т.д.), email-напоминаний (Resend/cron).

- **Phase 10 — Voting**  
  Нет кнопки голоса на карточках событий, бэкенда голосов (Vote, like/unlike, отображение счётчика).

- **Phase 11 — Gamification**  
  Профиль и бейджи есть; логика достижений не реализована (когда и какие разблокировать).

- **Phase 12 — PWA**  
  Нет next-pwa / service worker, manifest.json, офлайн-страницы, индикатора онлайн/офлайн.

- **Phase 13 — Polish**  
  Нет глобальных `error.tsx` и `loading.tsx`, скелетонов загрузки для списков/карточек, Error Boundary для критичных блоков.

### Документы (plan.md)

- Нет превью документа (PDF/картинки в модалке).
- Нет «привязать документ к событию».
- Нет удаления документа.

### Карта

- Нет подгрузки событий поездки с `lat/lng` и отображения их на карте вместо mock.

---

## 3. Документация и консистентность

- **README** указывает **Clerk** и **UploadThing**; в проекте фактически **Supabase (auth + storage)**. Привести README (и при необходимости plan.md) в соответствие со стеком.
- В **docs/plan.md** в фазах ещё упоминается Clerk — заменить на текущую схему аутентификации.

---

## 4. Качество и надёжность

- **Тесты:** нет `*.test.ts/tsx` — ни unit, ни e2e.
- **Ошибки:** нет `app/error.tsx` и `app/**/error.tsx`.
- **Загрузка:** нет `loading.tsx` в сегментах — нет единого скелетона при переходах.
- **Поиск по документам:** поле «Search files» на `/trip/[id]/documents` только UI — не подключено к фильтрации списка.

---

## 5. Приоритизированный список

### Высокий приоритет

1. Подключить **TripMap** к реальным событиям поездки (lat/lng из БД), убрать mock.
2. **Dashboard «Places Saved»** — считать из API/БД или временно убрать/подписать «Coming soon».
3. **Profile** — подтягивать текущего пользователя и его поездки из Supabase/сессии вместо mock.
4. Добавить **error.tsx** и при необходимости **loading.tsx** в `app` и ключевых маршрутах.

### Средний приоритет

5. **Документы:** превью (модалка PDF/image), удаление, опционально — привязка к событию.
6. **Уведомления:** модель + API + создание при действиях; опционально — Resend для напоминаний.
7. **Voting:** кнопка на карточке события + бэкенд голосов.
8. Подключить **поиск по документам** к реальной фильтрации.

### Ниже приоритет

9. **PWA:** manifest, service worker, базовая офлайн-страница и индикатор сети.
10. **Gamification:** правила разблокировки достижений и вызов при добавлении событий/документов.
11. **Скелетоны** загрузки для списков (trips, events, documents, notifications).
12. **README + plan.md:** привести в соответствие со стеком (Supabase вместо Clerk/UploadThing) и актуализировать чек-листы.
